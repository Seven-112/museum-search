{
  "name": "museum-search",
  "description": "Multi-container application providing a GraphQL search API for museum names and locations.",
  "scripts": {
    "postinstall": "cd ./server && npm install && cd ../client && npm install",
    "test": "jest --projects server",
    "test:coverage": "jest --coverage --projects server"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/PoffM/museum-search.git"
  },
  "author": "poffm",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/PoffM/museum-search/issues"
  },
  "homepage": "https://github.com/PoffM/museum-search#readme",
  "devDependencies": {
    "jest": "^23.6.0"
  }
}
